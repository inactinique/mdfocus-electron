# Configuration npm pour ClioDesk

# Forcer la reconstruction des modules natifs après installation
# Cela assure que les binaires correspondent toujours à l'architecture locale
rebuild-bundle=true

# Ne pas exécuter les scripts de build pendant l'installation des dépendances
# (sauf si explicitement demandé avec npm rebuild)
ignore-scripts=false
